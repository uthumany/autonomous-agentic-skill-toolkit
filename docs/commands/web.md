# Web Testing

## `aast test:web <url>`

Runs automated web tests on a specified URL using Playwright. This command navigates to the URL, takes a screenshot, and performs basic checks.

### Arguments

*   `<url>` (required): The URL of the web application to test.

### Examples

```bash
aast test:web https://example.com
```
